{
  "name": "ls-bet-bot",
  "version": "1.0.0",
  "description": "LS Bet Discord bot",
  "main": "index.js",
  "scripts": {
    "start": "npm run db && node index.js",
    "dev": "nodemon index.js",
    "commands": "node deploy-commands.js",
    "db": "npx prisma generate --schema=prisma/schema.prisma && npx prisma db push --schema=prisma/schema.prisma"
  },
  "keywords": [],
  "author": "LS Bet",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "discord.js": "^14.24.2",
    "dotenv": "^17.2.3",
    "prisma": "^6.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}